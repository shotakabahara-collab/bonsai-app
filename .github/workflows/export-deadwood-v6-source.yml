name: Export current Photoreal Craft v6 source

on:
  push:
    branches: [agent/photoreal-craft-v6-final]
    paths: ['.github/workflows/export-deadwood-v6-source.yml']
  pull_request:
    branches: [main]
    paths: ['.github/workflows/export-deadwood-v6-source.yml']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/photoreal-craft-v6-final
          fetch-depth: 1
      - name: Archive exact branch source
        run: |
          tar --exclude=.git --exclude=next/node_modules --exclude=next/dist --exclude=next/test-artifacts -czf /tmp/bonsai-v6-deadwood-source.tar.gz .
          sha256sum /tmp/bonsai-v6-deadwood-source.tar.gz > /tmp/bonsai-v6-deadwood-source.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: bonsai-v6-deadwood-source
          path: |
            /tmp/bonsai-v6-deadwood-source.tar.gz
            /tmp/bonsai-v6-deadwood-source.sha256
          if-no-files-found: error
          retention-days: 3
